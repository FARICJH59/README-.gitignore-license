name: GPT-5 Dashboard CI/CD

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 6 * * *" # Daily 06:00 UTC health/metrics run
  workflow_dispatch:

jobs:
  build-deploy-monitor:
    runs-on: ubuntu-latest
    env:
      # Defaults point to production projects; override via repository variables when needed.
      DEPLOY_REGION: us-central1
      CLOUD_RUN_PROJECTS: ${{ vars.CLOUD_RUN_PROJECTS || 'RUGGED-SILO-476415-P6,axiomcore-platform-642795357173' }}
      EMAIL_ALERT: farichva@gmail.com
      PRIMARY_GCP_PROJECT: ${{ vars.PRIMARY_GCP_PROJECT || 'RUGGED-SILO-476415-P6' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20 for frontend build
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build frontend with Vite/Tailwind
        run: npm run build
        working-directory: frontend

      - name: Setup Python 3.11 for backend
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Quick backend sanity check
        run: python -m compileall backend # Fast validation to catch syntax errors early

      - name: Determine Google Cloud auth method
        id: gcp-auth
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          GCP_WIF_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
        run: |
          if [[ -n "${GCP_SA_KEY}" ]]; then
            echo "method=sa-key" >> "$GITHUB_OUTPUT"
          elif [[ -n "${GCP_WIF_PROVIDER}" && -n "${GCP_SERVICE_ACCOUNT}" ]]; then
            echo "method=wif" >> "$GITHUB_OUTPUT"
          else
            echo "method=none" >> "$GITHUB_OUTPUT"
            echo "::notice::No Google Cloud credentials configured; deployment steps will be skipped."
          fi

      - name: Build Docker image (multi-stage)
        run: docker build -t gpt5-dashboard:${{ github.sha }} -f backend/Dockerfile .

      - name: Authenticate to Google Cloud (service account key)
        if: steps.gcp-auth.outputs.method == 'sa-key'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Authenticate to Google Cloud (workload identity)
        if: steps.gcp-auth.outputs.method == 'wif'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Setup gcloud CLI
        if: steps.gcp-auth.outputs.method != 'none'
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PRIMARY_GCP_PROJECT }}

      - name: Build and deploy container to Cloud Run
        if: steps.gcp-auth.outputs.method != 'none'
        env:
          CLOUD_RUN_SERVICE: gpt5-dashboard
        run: |
          IFS=',' read -ra projects <<< "$CLOUD_RUN_PROJECTS"
          for project in "${projects[@]}"; do
            image="gcr.io/${project}/gpt5-dashboard:${GITHUB_SHA}"
            # Build and push image per project to avoid cross-project ACL issues
            gcloud builds submit --project "$project" --tag "$image" --file backend/Dockerfile .
            # Deploy sequentially to keep rollouts deterministic
            gcloud run deploy "$CLOUD_RUN_SERVICE" \
              --image "$image" \
              --region "$DEPLOY_REGION" \
              --platform managed \
              --allow-unauthenticated \
              --port 8080 \
              --project "$project"
          done

      - name: Deploy frontend to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: gpt5-dashboard
          directory: frontend/dist

      - name: Run Cloud Run health monitor
        if: steps.gcp-auth.outputs.method != 'none'
        env:
          CLOUD_RUN_PROJECTS: ${{ env.CLOUD_RUN_PROJECTS }}
          DEPLOY_REGION: ${{ env.DEPLOY_REGION }}
          CLOUD_RUN_SERVICE: gpt5-dashboard
        run: |
          python backend/scripts/monitor.py

      - name: Collect usage and billing metrics
        if: steps.gcp-auth.outputs.method != 'none'
        env:
          CLOUD_RUN_PROJECTS: ${{ env.CLOUD_RUN_PROJECTS }}
          DEPLOY_REGION: ${{ env.DEPLOY_REGION }}
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          python backend/scripts/collect_metrics.py

      - name: Upload metrics artifact
        if: steps.gcp-auth.outputs.method != 'none'
        uses: actions/upload-artifact@v4
        with:
          name: metrics-json
          path: backend/scripts/metrics.json

      - name: Send daily email alert with metrics
        if: steps.gcp-auth.outputs.method != 'none'
        env:
          ALERT_EMAIL_USER: ${{ secrets.ALERT_EMAIL_USER }}
          ALERT_EMAIL_PASS: ${{ secrets.ALERT_EMAIL_PASS }}
          EMAIL_ALERT: ${{ env.EMAIL_ALERT }}
        run: |
          python - <<'PY'
          import json
          import os
          import smtplib
          import ssl
          from email.message import EmailMessage

          metrics_path = "backend/scripts/metrics.json"
          metrics = {}
          if os.path.exists(metrics_path):
              with open(metrics_path) as f:
                  metrics = json.load(f)

          msg = EmailMessage()
          msg["Subject"] = "GPT-5 Dashboard metrics"
          msg["From"] = os.environ["ALERT_EMAIL_USER"]
          msg["To"] = os.environ.get("EMAIL_ALERT")
          msg.set_content(f"Latest metrics payload:\n\n{json.dumps(metrics, indent=2)}")

          context = ssl.create_default_context()
          with smtplib.SMTP_SSL("smtp.gmail.com", 465, context=context) as server:
              server.login(os.environ["ALERT_EMAIL_USER"], os.environ["ALERT_EMAIL_PASS"])
              server.send_message(msg)
          PY
